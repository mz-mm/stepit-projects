<UserControl x:Class="Monefy.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Monefy.Views"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1200">
    <Grid>
        <StackPanel>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
                <DatePicker Margin="0,0,8,0"/>
                <Button Command="{Binding DayBackCommand}" Content="{md:PackIcon Kind=ArrowLeft}"/>
                <Label Content="{Binding CurrentDate}"/>
                <Button Command="{Binding DayForwardCommand}" Content="{md:PackIcon Kind=ArrowRight}"/>
            </StackPanel>
            <lvc:PieChart InnerRadius="50" Height="750" Series="{Binding ChartData}" IsEnabled="False" Width="500" LegendLocation="Bottom" Hoverable="False"/>
        </StackPanel>
    </Grid>
</UserControl>
